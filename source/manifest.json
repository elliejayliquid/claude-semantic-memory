{
  "dxt_version": "0.1",
  "name": "claude-memory",
  "display_name": "Claude's Semantic Memory System",
  "version": "1.1.0",
  "description": "A semantic memory system that lets Claude store and search memories using vector embeddings",
  "long_description": "This extension gives Claude a persistent semantic memory system. Memories are stored as vector embeddings, allowing Claude to search for related memories by meaning rather than just keywords. The system tracks retrieval patterns and boosts frequently accessed memories, mimicking how human memory works.\n\nMemories are stored in a configurable directory on your local machine and are never sent to external servers. This extension only accesses the directory you specify for storing memories - it does not access any other files on your computer.",
  "author": {
    "name": "Lighstromo Studios Ltd."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/elliejayliquid/claude-semantic-memory"
  },
  "server": {
    "type": "python",
    "entry_point": "mcp_server.py",
    "mcp_config": {
      "command": "python",
      "args": [
        "${__dirname}/mcp_server.py"
      ],
      "env": {
        "CLAUDE_MEMORIES_DIR": "${user_config.memories_directory}"
      }
    }
  },
  "tools": [
    {
      "name": "add_memory",
      "description": "Add a new memory with semantic embedding"
    },
    {
      "name": "search_memory",
      "description": "Search for semantically similar memories"
    },
    {
      "name": "list_memories",
      "description": "List recent memories"
    },
    {
      "name": "get_context_summary",
      "description": "Retrieve a 'Smart Context' of recent and high-importance memories"
    },
    {
      "name": "update_memory",
      "description": "Refine or correct an existing memory by its ID"
    },
    {
      "name": "visualize_memories",
      "description": "Launch the visual interstellar nebula dashboard for your memories"
    },
    {
      "name": "write_journal",
      "description": "Write a narrative journal entry with YAML frontmatter and companion memory"
    },
    {
      "name": "read_journal_latest",
      "description": "Read the latest curated journal entries for session orientation"
    },
    {
      "name": "search_journal",
      "description": "Search journal entries by semantic similarity"
    },
    {
      "name": "list_journal_entries",
      "description": "List all journal entries with metadata"
    },
    {
      "name": "pin_journal_entry",
      "description": "Pin or unpin a journal entry for the latest view"
    }
  ],
  "tools_generated": true,
  "keywords": [
    "memory",
    "semantic-search",
    "embeddings",
    "ai",
    "vector-db",
    "persistence",
    "journal"
  ],
  "license": "MIT",
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "python": ">=3.10.0"
    }
  },
  "user_config": {
    "memories_directory": {
      "type": "directory",
      "title": "Memories Directory",
      "description": "Where to store your semantic memories. This extension ONLY accesses this directory - no other files on your computer. All memories will be saved here as JSON files with embeddings.",
      "required": true,
      "default": "~/.claude-memories"
    }
  }
}